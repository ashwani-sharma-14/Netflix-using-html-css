type: web-service
env:
  NODE_ENV: production # Optional, for production builds
build_command: echo "No build step required" # Since you're using only HTML/CSS
start_command: echo "Static site served"
